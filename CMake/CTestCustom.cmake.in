SET(CTEST_CUSTOM_COVERAGE_EXCLUDE
  ${CTEST_CUSTOM_COVERAGE_EXCLUDE}

  # Exclude try_compile sources from coverage results:
  "/CMakeFiles/CMakeTmp/"

  # Exclude files generated by the moc pre-compiler
  ".*/moc_.*"

  # Exclude files generated by the uic pre-compiler
  ".*/ui_.*"

  # Exclude files from the Testing directories
  ".*/Testing/.*"

  # Exclude code in the ThirdParty directory
  ".*/ThirdParty/.*"
  )

# The following tests should not be run under valgrind
SET(CTEST_CUSTOM_MEMCHECK_IGNORE

  )

SET(CTEST_CUSTOM_WARNING_EXCEPTION
  ${CTEST_CUSTOM_WARNING_EXCEPTION}
  # VTK
  "VTK/Utilities/vtktiff/"
  "may be unsafe"
  "old-style cast"
  "The POSIX name for this item is deprecated"
  # Superbuild
  "commits behind, not connected"
  )
