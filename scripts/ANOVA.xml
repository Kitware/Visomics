<analysis name="ANOVA">
  <inputs>
    <input name="input_table" type="Table"/>
  </inputs>
  <outputs>
    <output name="ANOVA_table" type="Table" rawViewType="voTableView"
            rawViewPrettyName="Significance (Table)" />
    <output name="ANOVA_volcano" type="Table"
            viewType="voVolcanoView" viewPrettyName="Volcano Plot"
            rawViewType="voTableView" rawViewPrettyName="Volcano Table"/>
  </outputs>
  <parameters>
    <parameter name="sample1_range" type="Range">
      <title>Sample Group 1</title>
      <description>
         A group of Experiments, specified by a range and/or list of column letters.
         The groups MUST both be the same size, as only a balanced test is performed.
      </description>
      <default>A-C,F</default>
    </parameter>
    <parameter name="sample2_range" type="Range">
      <title>Sample Group 2</title>
      <description>
      A group of Experiments, specified by a range and/or list of column letters.
      The groups MUST both be the same size, as only a balanced test is performed.
      </description>
      <default>D,G-I</default>
    </parameter>
  </parameters>
</analysis>
